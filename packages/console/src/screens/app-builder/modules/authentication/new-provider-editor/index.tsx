import type { FunctionComponent, ReactElement } from "react";

const NewProviderEditor: FunctionComponent = (): ReactElement => {
    return <div>New Provider Editor</div>;
};

export default NewProviderEditor;
