import fs from "fs";
import yaml from "js-yaml";

import * as paths from "../config/paths";

const compiler = () => {
    console.log(paths.MANIFEST_DIRECTORY);
    return null;
};

export default compiler;
