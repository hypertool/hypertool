import type { FunctionComponent, ReactElement } from "react";

const ResourceLibrary: FunctionComponent = (): ReactElement => {
  return <section>View Resources</section>;
};

export default ResourceLibrary;
