import type { FunctionComponent, ReactElement } from "react";

const EditResource: FunctionComponent = (): ReactElement => {
  return <section>Edit Resource</section>;
};

export default EditResource;
