import type { FunctionComponent, ReactElement } from "react";

interface Props {}

const ViewApps: FunctionComponent<Props> = (): ReactElement => {
  return <section>View Apps</section>;
};

export default ViewApps;
